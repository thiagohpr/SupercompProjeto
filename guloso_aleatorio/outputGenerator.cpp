#include<iostream>
#include <chrono>
#include<vector>
#include<algorithm>
#include <random>
#include <map>
#include <fstream>

using namespace std;

struct filme {
    int id;
    int h_inicio;
    int h_fim;
    int categoria;
    int duracao;
};
int main(){
    ofstream outputFile;
    outputFile.open("input_"+to_string(n)+"_"+to_string(m));
    outputFile << n << " " << m << endl;
}
    
    